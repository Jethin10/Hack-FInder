{
  "name": "hack-finder",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd app && npm install",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "start:api": "cd app && npm run start:api",
    "dev": "cd app && npm run dev"
  }
}
